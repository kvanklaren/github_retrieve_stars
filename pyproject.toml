[project]
name = "rp-github-stars"
version = "0.1.0"
description = ""
authors = [
    {name = "Koen van Klaren",email = "k.vanklaren@sana-commerce.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv (>=1.2.1,<2.0.0)",
    "requests (>=2.32.5,<3.0.0)"
]

[tool.poetry]
packages = [{include = "rp_github_stars", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
